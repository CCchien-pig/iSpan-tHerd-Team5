@model tHerdBackend.UIKit.Rcl.ViewComponents.UpdateImage.UpdateImageViewModel

@{
    bool isExternal = false;
    if (Model.File?.IsExternal is bool b)
        isExternal = b;
    else if (Model.File?.IsExternal.ToString().ToLower() == "true")
        isExternal = true;
}

<div class="modal fade" id="@Model.ModalId" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@Model.Title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body text-center">
                <img id="modalImg"
                     src="@Model.File.FileUrl"
                     alt="@Model.File.AltText"
                     class="img-fluid mb-3 rounded img-zoomable"
                     style="max-width:600px; max-height:400px; object-fit:contain; cursor: zoom-in;"
                     data-file-id="@Model.File.FileId"
                     data-alt="@Model.File.AltText"
                     data-caption="@Model.File.Caption"
                     data-is-active="@Model.File.IsActive.ToString().ToLower()"
                     data-update-api="@Model.UpdateApiUrl"
                     data-delete-api="@Model.DeleteApiUrl" />

                <div class="row g-4">
                    <!-- 左側：檔案資訊 -->
                    <div class="col-md-6">
                        <div class="card shadow-sm border-0 h-100">
                            <div class="card-header bg-light fw-bold">
                                📁 檔案資訊
                            </div>
                            <div class="card-body">
                                <div class="mb-3 text-start">
                                    <label class="form-label fw-semibold">檔案代號 (Cloudinary 產生)</label>
                                    <input type="text" id="modalFileKey" class="form-control" value="@Model.File.FileKey" readonly />
                                </div>

                                <div class="row">
                                    <div class="col-6 mb-3 text-start">
                                        <label class="form-label fw-semibold">寬度 (px)</label>
                                        <input type="number" id="modalWidth" name="Width" class="form-control" value="@Model.File.Width" />
                                    </div>
                                    <div class="col-6 mb-3 text-start">
                                        <label class="form-label fw-semibold">高度 (px)</label>
                                        <input type="number" id="modalHeight" name="Height" class="form-control" value="@Model.File.Height" />
                                    </div>
                                </div>

                                <div class="mb-3 text-start">
                                    <label class="form-label fw-semibold">檔案大小</label>
                                    <input type="text" id="modalFileSizeBytes" class="form-control" value="@Model.File.FileSizeBytes + " Bytes"" readonly />
                                </div>

                                <div class="mb-3 text-start">
                                    <label class="form-label fw-semibold">MIME 類型</label>
                                    <input type="text" id="modalMimeType" class="form-control" value="@Model.File.MimeType" readonly />
                                </div>

                                <div class="mb-3 text-start">
                                    <label class="form-label fw-semibold">建檔時間</label>
                                    <input type="text" id="modalCreatedDate" class="form-control" value="@Model.File.FormateCreatedDate?.ToString()" readonly />
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 右側：編輯資訊 -->
                    <div class="col-md-6">
                        <div class="card shadow-sm border-0 h-100">
                            <div class="card-header bg-light fw-bold">
                                📝 說明與狀態
                            </div>
                            <div class="card-body">
                                <div class="mb-3 text-start">
                                    <label for="modalFileId" class="form-label fw-semibold text-danger">檔案編號</label>
                                    <input type="text" id="modalFileId" class="form-control" value="@Model.File.FileId" readonly />
                                </div>

                                <div class="mb-3 text-start">
                                    <label for="modalAlt" class="form-label fw-semibold">AltText（圖片替代文字）</label>
                                    <input type="text" id="modalAlt" class="form-control" value="@Model.File.AltText" />
                                </div>

                                <div class="mb-3 text-start">
                                    <label for="modalCaption" class="form-label fw-semibold">Caption（說明文字）</label>
                                    <textarea id="modalCaption" class="form-control" rows="3">@Model.File.Caption</textarea>
                                </div>

                                <div class="form-check form-switch mb-3">
                                    <input class="form-check-input" type="checkbox" id="modalIsActive" @(Model.File.IsActive ? "checked" : "") />
                                    <label class="form-check-label fw-semibold" for="modalIsActive">是否啟用</label>
                                </div>

                                <div class="mb-3 d-flex align-items-center">
                                    <label class="form-label fw-semibold me-2">是否外部連結：</label>
                                    <span class="badge @(isExternal ? "bg-success" : "bg-secondary") fs-6">
                                        @(isExternal ? "外部連結" : "自有檔案")
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>


            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                <button type="button" id="confirmMetaBtn" class="btn btn-primary">確定</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("/Views/Shared/_SweetAlertPartial.cshtml")

    <script>
        document.addEventListener("shown.bs.modal", (event) => {
            const modal = event.target;
            const img = modal.querySelector(".img-zoomable");
            if (!img) return;

            img.addEventListener("click", (e) => {
                e.preventDefault();
                e.stopPropagation();
                window.open(img.src, "_blank");
            });
        });
    </script>
}

